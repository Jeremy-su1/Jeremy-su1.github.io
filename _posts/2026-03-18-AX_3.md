---
layout: single
title: "AI Advanced 교육 2일차"
categories: AI
tag: [AI, ML, DeepLearning]
toc: true
toc_sticky: true
Typora-root-url: ../
author_profile: false
sidebar:
  nav: "docs"
search: true
classes: wide
---
# 교육 3일차 (정리중....🔨)

## Summary
parsing/chunking/embedding ➡️ 검색에 포함

Q: workspace를 만드는 이유?
A: workspace 생성은 parsing -> 
    opensearch에 인덱스를 만든다. -> 한국어/영어에 따라서 노리 분석기를 사용하는 게 있다.

images/2026-03-18-AX_3/Screenshot 2026-03-18 at 09.33.49.png

## Parsing / Chunking / Embedding
graphQL

```
> 📕 **워크스페이스 생성 파이프라인 — 인프라 + 비즈니스 로직 통합 다이어그램**
>
> 쉘 스크립트(또는 프론트엔드 UI)에서 워크스페이스를 생성할 때, 내부적으로 어떤 AWS 인프라를 거쳐 어떤 로직이 실행되는지 보여줍니다.
>
> ```mermaid
> graph TB
>     subgraph CLI["Shell Script / Frontend UI"]
>         A["<i>create-opensearch-workspace.sh</i><br/>or Frontend UI"]
>     end
>
>     subgraph Auth["Authentication"]
>         B["Cognito User Pool<br/><i>initiate-auth</i> / JWT Token"]
>     end
>
>     subgraph API["GraphQL API"]
>         C["AppSync<br/><i>createOpenSearchWorkspace</i> mutation"]
>         D["Lambda <i>api-handler</i><br/><i>routes/workspaces.py</i><br/>"]
>         API_NOTE["CDK: <i>ChatBotApi</i>"]
>     end
>
>     subgraph SDK["Workspace SDK"]
>         E["<i>create_workspace_open_search</i><br/><br/>Verify embedding model<br/>DynamoDB put_item<br/>status = submitted"]
>         H["SFN start_execution<br/><i>CreateOpenSearchWorkspace</i>"]
>         SDK_NOTE["<i>genai_core/workspaces.py</i>"]
>     end
>
>     subgraph SFN["Step Functions Workflow"]
>         I["<i>SetCreating</i><br/>DynamoDB: status to creating"]
>         J["Lambda <i>create-workflow</i><br/><i>create_workspace_index</i>"]
>         K["<i>SetReady</i><br/>DynamoDB: status to ready"]
>         ERR["<i>HandleError</i><br/>DynamoDB: status to error"]
>     end
>
>     subgraph Index["OpenSearch Index Creation"]
>         L["<i>HNSW k-NN Vector Index</i><br/>engine faiss / nmslib<br/>space_type l2 / cosine"]
>         M["<i>Nori Analyzer</i><br/>Korean morphological analysis"]
>         N["<i>Field Mappings</i><br/>content_embeddings knn_vector<br/>content / metadata / chunk_id"]
>     end
>
>     subgraph Store["Data Stores"]
>         O[("OpenSearch Serverless<br/>Collection")]
>         P[("DynamoDB<br/>Workspaces Table")]
>     end
>
>     A --> B --> C --> D --> E
>     E --> H
>     H --> I --> J
>     J --> L
>     J --> M
>     J --> N
>     L --> O
>     M --> O
>     N --> O
>     J --> K
>     K --> P
>     J -.-> ERR
>     ERR --> P
> ```
>
> 📌 **CDK 코드 위치**: `lib/chatbot-api/rest-api.ts` (GraphQL + Lambda),
> `lib/rag-engines/opensearch-vector/create-opensearch-workspace.ts` (Step Functions)
>
> 📌 **비즈니스 로직 코드 위치**: `routes/workspaces.py` (API 핸들러),
> `genai_core/workspaces.py` (DynamoDB 기록 + SFN 시작),
> `genai_core/opensearch/create.py` (인덱스 생성)
```


workspace를 만든다?의 개념은 위의 로직으로 수행이 된다.
```
> 📕 **워크스페이스 생성 상태 흐름 정리**
>
> ```
>   submitted ──▶ creating ──▶ ready            
>                          ╲                    
>                           ╲──▶ error (실패 시)  
> ```
>
> 우리가 실행한 쉘 스크립트 `create-opensearch-workspace.sh`는 위 다이어그램의 **맨 위 (Shell Script)** 에 있는 단계입니다.
> 스크립트에서는 Cognito 인증 → GraphQL API 호출을 수행하고, 그 후의 나머지 흐름은 자동으로 진행됩니다.
> 워크스페이스 상태가 `ready`가 되면 문서를 적재할 수 있습니다.
```

- workspace -> meta data를 저장하기 위해 만드는 저장 공간이라고 보면 된다.
- images/2026-03-18-AX_3/image.png
1) parse_document: parsing 3개 전략 -> 문서 특성에 따라 선택 필요
    . default: S3FileLoader, customer PDF: CV layout, ragx: MinerU
2) split_content: chunking 전략 4개
    . 재귀적(recursive) / 시멘틱(semantic) / 계층적(hierarchical) - 문맥고려 / ragx - multimodal chunking
3) store_chunks: embedding
    . 1 chunk -> 1 embeding
```
> 📕 **file_import_job_main.py — 핵심 비즈니스 로직 흐름**
>
> 이 파일은 문서 처리 파이프라인의 핵심입니다. 아래 3단계를 순서대로 수행합니다.
>
> ```
> ┌──────────────────────────────────────────────────────────────────────┐
> │                      file_import_job_main.py                         │
> │                                                                      │
> │  ① parse_document()  ──▶  ② split_content()  ──▶  ③ store_chunks()  │
> │     (파싱)                   (청킹)                  (임베딩+저장)        │
> │                                                                      │
> │  파싱 전략 선택:                청킹 전략 선택:           임베딩 모델로        │
> │  - default (S3FileLoader)     - recursive          벡터 변환 후         │
> │  - custom_pdf                 - semantic           OpenSearch에       │
> │  - ragx                       - hierarchical       인덱싱              │
> │                               - ragx                                 │
> └──────────────────────────────────────────────────────────────────────┘
> ```
>
> 📕 **LangChain 개념 — `S3FileLoader`**
> - LangChain Community에서 제공하는 문서 로더입니다.
> - S3 버킷에 저장된 파일을 읽어 LangChain `Document` 객체로 변환합니다.
> - 파일 확장자에 따라 적절한 파서를 자동 선택합니다 (PDF → `PyPDFLoader`, DOCX → `Docx2txtLoader` 등).
```


- PDF를 parsing하기 위한 어려운 점
    . 여러 형태의 parsing -> computer vision으로 해결
    . -> box 로직 -> 다단 구성 확인 -> reading order
    . customPDFLoader-> _process_page를 통해서 확인 가능

- chunking logic 
    . S3fileLoader / CustomerPDFLoader / 

    parse_document
    . src/lambda/python-sdk/python/genai_core/parsing/parsers/pdf_loader.py


https://github.com/HKUDS/RAG-Anything

rsync -av --progress --exclude=.git vscode-server-ax-on-mastery:~/ax-on-mastery/ ./




### 한글 분석기 - Nori
- Text 필드 저장(인덱싱) 방식
    . 토크나이저
    . 토큰 필터
> 👉 Tokenizer = "쪼개는 역할"
> 👉 Token Filter = "가공하는 역할"
> 2️⃣ Tokenizer (토크나이저)
✔ 정의

문장을 "토큰(Token)" 단위로 분리하는 단계

가장 중요한 단계다.
여기서 잘못 쪼개면 이후 모든 검색 품질이 망가진다.


Hybrid 검색: 시맨틱 + 키워드 검색 옵션이다.



```python
    retrieve_documents: RetrieverOutputLike = RunnableBranch(
        (
            # Both empty string and empty list evaluate to False
            lambda x: not x.get("chat_history", False),
            # If no chat history, then we just pass input to retriever
            (lambda x: x["input"]) | retriever,
        ),
        # If chat history, then we pass inputs to LLM chain, then to retriever
        prompt | llm | StrOutputParser() | retriever,
    ).with_config(run_name="chat_retriever_chain")
    return retrieve_documents
```

retriever chain 
-> context 증강을 위해 QA chain을 만듬
-> invoke -> get relevant documents

1. 호출하려는 모델에 따라 adapter가 있으면 좋다.
2. retriever를 잘 구현해야된다 -> search logic쪽 -> hybrid search / keywork search
    - semantic search 








semantic chunking
keyword chunking
![parsing](<../images/2026-03-18-AX_3/Screenshot 2026-03-18 at 17.49.15.png>)
